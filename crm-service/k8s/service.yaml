apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: bookstore-ns
spec:
  selector:
    app: crm-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
